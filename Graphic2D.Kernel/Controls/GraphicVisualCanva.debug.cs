using Graphic2D.Kernel.Visuals;
using System;
using System.Windows;

namespace Graphic2D.Kernel.Controls
{
    public partial class GraphicVisualCanvas
    {
        //internal void SelectionMove(Vector delta)
        //{
        //    if (VisualSelection.Count > 0)
        //    {
        //        // 图形移动距离保留两位小数
        //        delta.X = Math.Round(delta.X, 2);
        //        delta.Y = Math.Round(delta.Y, 2);            

        //        // 移动图形
        //        foreach (GraphicVisual gv in VisualSelection )
        //        {
        //            VisualHelper.Move(gv, delta);
        //        }
        //        VisualSelection.UpdateSelection();
        //    }
        //}

        //internal void SelectionRotate(double delta, Point center)
        //{

        //}

        //internal void SelectionResize(Rect rect)
        //{

        //}

    }
}
